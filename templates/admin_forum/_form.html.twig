
{{ form_start(form) }}
    <div class="form-group">
        {{ form_widget(form.title, {'attr': {'placeholder': 'titre'}}) }}
        {{ form_label(form.title, 'Titre') }}
    </div>
    <div class="form-group">
        {{ form_widget(form.game) }}
        {{ form_label(form.game, 'Titre') }}
    </div>
    {{ form_row(form.submit, {
        'label':'Confirmer',
        'attr': {'class': 'btn'}
    }) }}
{{ form_end(form) }}