{% extends '/layout/user.html.twig' %}

{% block title %}Forum{% endblock %}

{% block body %}



    <div class="container">
        <h1 class="title">Forum</h1>
        <ul class="items-crud">
                <li class="forum-category post-category">
                    <span>{{ forum.title }}</span>
                    <div class="buttons">
                        <a class="btn btn-success" href="{{ path('admin_forum_edit', {'id': forum.id}) }}">↑</a>
                        {{ include('admin_forum/_delete_form.html.twig') }}

                    </div>
                </li>
        </ul>
        <a style="margin-top: 2rem" class="btn" href="{{ path('admin_forum_new') }}">Créer</a>
        <a class="btn" href="{{ path('admin_forum_index') }}">Retour à la liste</a>

    </div>
{% endblock %}
